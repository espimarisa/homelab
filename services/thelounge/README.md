# Useful Commands

- List users: `docker exec -it thelounge thelounge list`
- Create a user: `docker exec -it thelounge thelounge add <username>`
- Delete a user: `docker exec -it thelounge thelounge remove <username>`
- Reset a user's password: `docker exec -it thelounge thelounge reset <username>`
