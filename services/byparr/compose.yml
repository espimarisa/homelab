services:
  ###########################################################################
  # Byparr: Cloudflare solver - https://github.com/ThePhaseless/Byparr      #
  ###########################################################################

  byparr:
    image: ghcr.io/thephaseless/byparr:c6b50596e21c6a5c7b4886b82dff913dc120d0f8-amd64
    container_name: byparr
    network_mode: service:gluetun
    restart: unless-stopped
    volumes:
      - /etc/localtime:/etc/localtime:ro
    cap_drop:
      - AUDIT_WRITE
      - MKNOD
      - SYS_ADMIN
      - SYS_CHROOT
      - SYS_PTRACE
    depends_on:
      gluetun:
        condition: service_healthy
        restart: true
    deploy:
      resources:
        limits:
          cpus: 1
          memory: 2048M
    security_opt:
      - no-new-privileges:true
