---
# Default container options.
x-defaults: &defaults
  read_only: true
  restart: unless-stopped
  environment:
    TZ: "${TZ}"
  cap_drop:
    - ALL
  security_opt:
    - no-new-privileges:true

services:
  # qBittorrent: BitTorrent client. https://github.com/qbittorrent/qBittorrent/wiki
  qbittorrent:
    <<: *defaults
    image: ghcr.io/11notes/qbittorrent:5.2
    container_name: qbittorrent
    network_mode: service:gluetun
    volumes:
      - ${DOWNLOADS_INCOMPLETE_PATH}:/downloads-incomplete:rw
      - ${DOWNLOADS_PERMASEED_PATH}:/downloads-permaseed:rw
      - ${DOWNLOADS_PATH}:/downloads:rw
      - qbittorrent-config:/qbittorrent/etc:rw
      - qbittorrent-data:/qbittorrent/var:rw
      - qbittorrent-themes:/qbittorrent/themes:rw
    depends_on:
      gluetun:
        condition: service_healthy
        restart: true
    deploy:
      resources:
        limits:
          cpus: 4
          memory: 4GB

  # qui: qBittorrent management web interface. https://github.com/autobrr/qui
  qui:
    <<: *defaults
    image: ghcr.io/autobrr/qui:v1.20
    container_name: qui
    hostname: qui
    user: "${PUID}:${PGID}"
    expose:
      - 7476/tcp
    networks:
      - external
      - gluetun
    volumes:
      - qui:/config:rw
    depends_on:
      qbittorrent:
        condition: service_healthy
        restart: true
    deploy:
      resources:
        limits:
          cpus: 1
          memory: 512M

networks:
  external:
    name: external-network
    external: true
  gluetun:
    name: gluetun-network
    external: true

volumes:
  qbittorrent-config:
    name: qbittorrent-config-volume
    external: true
  qbittorrent-data:
    name: qbittorrent-data-volume
    external: true
  qbittorrent-themes:
    name: qbittorrent-themes-volume
    external: true
  qui:
    name: qui-volume
    external: true
